# PRD (Product Requirements Document)
## 마이 헬스 로그 API

---

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 마이 헬스 로그 API (My Health Log API) |
| 작성자 | 이연주 |
| 작성일 | 2026.07.24 |
| 버전 | 1.0 |

---

## 2. 프로젝트 개요

### 2.1 배경

건강 관리에 관심이 있는 사람들은 매일 체중, 혈압, 혈당 등을 기록하지만, 기록만 할 뿐 그 수치가 의미하는 건강 상태를 스스로 판단하기 어렵다. 이 프로젝트는 사용자가 입력한 건강 수치를 자동으로 분석하고 분류하여, 누구나 쉽게 자신의 건강 상태를 파악할 수 있도록 돕는 API를 제공하는 것을 목표로 한다.

### 2.2 한 줄 요약

> 매일의 건강 수치(몸무게·키·혈압·혈당)를 기록하면, 서버가 BMI를 자동 계산하고 건강 상태를 분류하며, 누적 기록으로 통계를 제공하는 REST API

### 2.3 목적

- 건강 기록을 손쉽게 저장, 조회, 수정, 삭제할 수 있는 API 제공
- 저장 시 BMI·혈압·혈당을 자동으로 계산 및 분류
- 위험 범위에 해당하는 경우 경고 메시지 자동 생성
- 기간별 검색 및 통계(평균 체중, 평균 BMI 등) 제공
- Docker 기반으로 어떤 환경에서도 동일하게 실행 가능하도록 배포

---

## 3. 타겟 사용자

| 사용자 유형 | 니즈 |
|---|---|
| 건강 관리에 관심 있는 일반 사용자 | 매일의 건강 수치를 기록하고, 위험 신호를 빠르게 인지하고 싶음 |
| 개발 학습자(본인) | FastAPI, Docker를 활용한 REST API 설계·구현·배포 전체 흐름 학습 |

---

## 4. 핵심 기능 (Functional Requirements)

### 4.1 건강 기록 CRUD

| 기능 | 설명 |
|---|---|
| 기록 추가 | 건강 수치를 입력받아 저장하고, BMI·분류·경고를 계산하여 응답 |
| 전체 조회 | 저장된 모든 기록을 개수와 함께 조회 |
| 단건 조회 | 특정 id의 기록을 조회 (없으면 404 반환) |
| 기록 수정 | 특정 id의 기록 내용을 수정하고 재계산 |
| 기록 삭제 | 특정 id의 기록 삭제 |

### 4.2 자동 건강 분석

| 항목 | 설명 |
|---|---|
| BMI 계산 | 몸무게(kg) ÷ 키(m)² 공식으로 자동 계산 |
| BMI 분류 | 저체중 / 정상 / 과체중 / 비만 4단계 분류 |
| 혈압 분류 | 정상 / 주의 / 고혈압 3단계 분류 |
| 혈당 분류 | 정상 / 공복혈당장애 / 당뇨 의심 3단계 분류 |
| 경고 생성 | 비만·고혈압·당뇨 의심 시 경고 메시지 자동 생성 |
| 걸음 수 등급 *(추가 구현)* | 부족 / 적정 / 우수 3단계로 활동량 분류 |

### 4.3 검색 및 통계

| 기능 | 설명 |
|---|---|
| 날짜 범위 검색 | 시작일~종료일 범위에 해당하는 기록만 조회 |
| 통계 조회 | 전체 기록 수, 평균 체중, 평균 BMI 제공 |

### 4.4 데이터 영속성

- 기록은 서버 재시작 후에도 유지되어야 하며, 이를 위해 파일(JSON) 기반으로 저장

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 |
|---|---|
| 데이터 검증 | 모든 요청 본문은 Pydantic 모델로 형식을 검증 |
| 문서화 | `/docs` 경로에서 Swagger UI로 모든 기능 테스트 가능 |
| 안정성 | 예외 상황(존재하지 않는 id 조회, 기록 0건일 때 통계 등)에서도 서버가 죽지 않고 적절히 응답 |
| 이식성 | Docker 컨테이너로 패키징하여 어떤 환경에서도 동일하게 실행 |
| 배포 | AWS EC2에 Docker 컨테이너로 배포하여 외부에서 접속 가능 |

---

## 6. 데이터 필드 정의

| 필드명 | 타입 | 필수 여부 | 설명 |
|---|---|---|---|
| date | str | 필수 | 측정일 (예: "2026-07-20") |
| weight | float | 필수 | 몸무게(kg) |
| height | float | 필수 | 키(cm) |
| systolic | int | 필수 | 수축기 혈압 |
| diastolic | int | 필수 | 이완기 혈압 |
| blood_sugar | int | 필수 | 공복 혈당(mg/dL) |
| steps | int | 선택 (기본값 0) | 걸음 수 |
| sleep_hours | float | 선택 (기본값 0.0) | 수면 시간 |
| memo | str | 선택 (기본값 "") | 메모 |

응답 시 서버가 자동 계산하여 추가하는 필드: `id`, `bmi`, `bmi_category`, `bp_category`, `sugar_category`, `warnings`, `steps_category`

---

## 7. 분류 기준

### BMI (kg ÷ m²)
| 구간 | 분류 |
|---|---|
| 18.5 미만 | 저체중 |
| 18.5 ~ 22.9 | 정상 |
| 23 ~ 24.9 | 과체중 |
| 25 이상 | 비만 |

### 혈압
| 구간 | 분류 |
|---|---|
| 수축기 <120 그리고 이완기 <80 | 정상 |
| 120~139 또는 80~89 | 주의 |
| 수축기 ≥140 또는 이완기 ≥90 | 고혈압 |

### 공복혈당
| 구간 | 분류 |
|---|---|
| 100 미만 | 정상 |
| 100 ~ 125 | 공복혈당장애 |
| 126 이상 | 당뇨 의심 |

### 걸음 수 (추가 구현)
| 구간 | 등급 |
|---|---|
| 5,000 미만 | 부족 |
| 5,000 ~ 9,999 | 적정 |
| 10,000 이상 | 우수 |

> ※ 위 건강 분류 기준은 학습을 위해 단순화한 값으로, 실제 의학적 진단 기준이 아닙니다.

---

## 8. API 명세 요약

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | /records | 건강 기록 추가 (BMI·분류·경고·걸음 수 등급 자동 계산) |
| GET | /records | 전체 기록 조회 |
| GET | /records/{id} | 기록 단건 조회 |
| PUT | /records/{id} | 기록 수정 |
| DELETE | /records/{id} | 기록 삭제 |
| GET | /search?start=&end= | 날짜 범위 검색 |
| GET | /stats | 통계(평균 체중, 평균 BMI 등) 조회 |

---

## 9. 기술 스택

| 구분 | 기술 |
|---|---|
| 언어 | Python 3.11 |
| 프레임워크 | FastAPI |
| 데이터 검증 | Pydantic |
| 서버 실행 | Uvicorn |
| 데이터 저장 | JSON 파일 |
| 컨테이너화 | Docker |
| 배포 | AWS EC2 |

---

## 10. 성공 기준 (Definition of Done)

- [ ] 7개 엔드포인트가 모두 정상 동작한다
- [ ] BMI·혈압·혈당 분류 및 경고 메시지가 기준표대로 정확히 계산된다
- [ ] 서버 재시작 후에도 데이터가 유지된다
- [ ] `docker build`, `docker run`이 성공적으로 실행된다
- [ ] AWS 서버 배포 후 외부에서 `/docs` 접속이 가능하다
- [ ] GitHub 저장소가 Public 상태로 최종 코드까지 push되어 있다
